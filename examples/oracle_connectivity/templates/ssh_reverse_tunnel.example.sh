#!/usr/bin/env bash
# SSH reverse tunnel example for manual review only.
# Do not run this file as part of Phase 24I.
# All commands are commented out. Use placeholders only.
# Warning: macOS sleep/network changes can break tunnels and require monitoring.
# No broker API is called. No real order is created or transmitted.
# order_execution_allowed=false

# Example concept only:
# ssh -N -R <oracle-forward-port>:127.0.0.1:8000 <oracle-user>@<oracle-host> -i <path-to-private-key>

echo "This is a commented example. No tunnel is started."
